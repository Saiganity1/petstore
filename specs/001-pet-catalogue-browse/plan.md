# Implementation Plan: Browse Pet Catalogue with Category Filtering

Summary: Implement backend endpoints `GET /api/pets` and `GET /api/pets/{id}`. Frontend renders catalogue and detail views using React + MUI + Tailwind. Seed data delivered via Flyway migration.

Project structure recommendations and constitution checks mirror the teacher's plan: backend Maven Spring Boot, frontend React Vite, Flyway migrations, DTO separation, and test-first approach.
