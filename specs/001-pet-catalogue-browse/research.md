# Research: Browse Pet Catalogue with Category Filtering

Includes decisions on pagination (server-side Pageable), category filtering via repeated query params, frontend state via URL search params (`useSearchParams`), use of React Query for data fetching, and image storage strategy (external URLs with frontend fallback).

The document lists rationale and alternatives for the key architecture choices.
