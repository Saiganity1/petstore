# Tasks: Browse Pet Catalogue with Category Filtering

This file contains the task breakdown used by the Spec-kit workflow: Setup, Foundational, User Stories (US1-US3), and Polish. Tasks include creating Flyway migrations, DTOs, repository methods, React Query hooks, components (PetCard, PetGrid), and tests.

See `plan.md` and `quickstart.md` for execution steps and checkpoints.
